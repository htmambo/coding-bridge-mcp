[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "astronmcp"
version = "0.1.0"
description = "MCP server wrapping iFlytek Spark API for code/plan review."
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "mcp[cli]>=1.20.0",
    "httpx>=0.27.0",
    "websockets>=12.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0",
]
authors = [
    { name = "astronmcp" },
]
license = { text = "MIT" }
keywords = ["mcp", "iflytek", "spark", "code-review", "plan-review"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.scripts]
astronmcp = "astronmcp.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/astronmcp"]

[tool.hatch.build.targets.sdist]
include = ["src/astronmcp", "README.md", "LICENSE"]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
    "pytest-asyncio>=1.4.0",
]
